/**
 * CONTEXT API
 * 1. create context (export)
 * 2. create provider
 * 3. set context value
 *4. to access the value: useContext hook
 */

/**
 * 1. set AuthProviders in main.jsx
 * 2. inside provider access children props & then use
 */